/*
#pragma once
#include <fstream>
#include <vector>
#include "Player.h"
#include "Enemy.h"

class GameStateManager {
public:
    static void SaveGame(Player& player, std::vector<Enemy*>& enemies);
    static void LoadGame(Player& player, std::vector<Enemy*>& enemies);
};
*/
